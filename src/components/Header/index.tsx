export const Header = () => {
  return (
    <header>
      <h1>Pokedex</h1>

      {/* <button className="cursor-pointer" onClick={toggleTheme}>
        <img
          className="w-8 h-8"
          src={`${themeConfig[theme].icon}`}
          alt="alternar tema"
        />
      </button> */}
    </header>
  );
};
